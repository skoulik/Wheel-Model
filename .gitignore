# Private brokerage statements — never commit
statements/

# Cached market data (code/prices.py) — refetchable, not ours to redistribute
data/

# Python bytecode cache (analyze_statement imports verify_examples)
__pycache__/

# Downloaded papers for the prior-work pass — reference only, not ours to redistribute
literature/
